# URL do backend Hono (server-side — sem expor ao browser)
API_URL="http://localhost:4000"

# Mesmo secret que o backend usa para assinar o JWT
JWT_SECRET="troque-para-um-secret-seguro-em-producao"

# Public key do Mercado Pago (checkout transparente). Vazia = pagamento
# simulado em dev (gateway fake no backend).
NEXT_PUBLIC_MP_PUBLIC_KEY=""
